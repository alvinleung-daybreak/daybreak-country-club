import React from "react";

type Props = {};

const Divider = (props: Props) => {
  return (
    <div className="border-t border-dotted max-w-[36ch] md:max-w-[70ch] mx-auto px-24 w-full border-[#999]" />
  );
};

export default Divider;
